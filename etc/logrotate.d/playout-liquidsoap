/var/log/playout/*liquidsoap*.log {
	rotate 7
	daily
	compress
	missingok
	copytruncate
#	postrotate
#	kill -HUP `cat /var/run/playout/playout-liquidsoap.pid 2>/dev/null` 2>/dev/null || true
#	endscript
}
